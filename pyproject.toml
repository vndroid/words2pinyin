[project]
name = "words2pinyin"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "bottle>=0.13,<0.14",
    "pypinyin",
    "pyyaml",
    "gunicorn",
]

[dependency-groups]
dev = [
    "ruff>=0.16.2",
]
